# NetAudit

NetAudit is a deterministic Cisco network configuration and IP audit engine designed to parse network device configs, validate interface configurations, detect IP and subnet discrepancies, and generate clear audit reports.

**Status:** Parser MVP in progress
